name: 🚀 Deploy to EC2

on:
  push:
    branches:
      - main
  workflow_dispatch: # Permite ejecutar manualmente

jobs:
  deploy:
    name: 🚀 Deploy to Production
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: 🔐 Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: 🚀 Deploy to server
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'ENDSSH'
            set -e
            
            echo "📁 Navegando al directorio de la aplicación..."
            cd /var/www/laravel
            
            echo "🔄 Actualizando código desde Git..."
            git fetch origin
            git reset --hard origin/main
            git clean -fd
            
            echo "📦 Ejecutando script de despliegue..."
            chmod +x deploy.sh
            ./deploy.sh
            
            echo "✅ Despliegue completado exitosamente"
          ENDSSH

      - name: ✅ Verificar despliegue
        run: |
          echo "🎉 Despliegue completado. Verifica en: https://emotive.g3nia.com"
